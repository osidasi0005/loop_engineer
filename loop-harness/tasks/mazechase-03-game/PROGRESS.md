# 進捗メモ

このファイルは反復をまたぐ唯一の「記憶」です。
エージェントは毎回ここを読み、作業の最後に追記します。ランナーも検証結果を 1 行ずつ追記します。

### 反復 1

**やったこと**
- `src/game.mjs` を新規作成（定数 7 個、`createGame`、`step` を export）。既存の `src/*.mjs` と `test/` は未変更。
- `step` は仕様の 1〜9 の順序どおりに実装。作業用オブジェクト `w` を浅いコピーで作り、配列・オブジェクトは差し替えのみで元の state を破壊しない。
- 衝突処理は `resolveCollisions(w)` に切り出し、衝突判定 A で残機が減ったら敵の移動と判定 B を飛ばす。
- ローカルで `node --test test/maze.test.mjs test/ghost.test.mjs test/game.test.mjs` を実行 → 54/54 PASS。

**分かったこと**
- 勝利判定は仕様どおり「`countPellets === 0` なら won」だけだと、餌が 0 個の迷路 `M6 = ['#####','#P G#','#####']` で初手から won になり、
  テスト「通常の敵に触れると残機が減り〜」（`status === 'playing'` を期待）が落ちる。
  → **その手で餌を食べた場合（`eaten !== 0`）だけ**勝利判定するようにした。
- 手順 7 の「`frightenedTimer` が 0 のときだけ `modeTimer` を減らす」は**減算前の値**で判定する。
  逃走タイマーが 1→0 になった手では `modeTimer` を減らさない（テスト「逃走中は全体モードのタイマーが止まり〜」が modeTimer 20 を期待）。
- 逃走終了時の復帰は反転なし（`{...g, mode}`）、全体モード切替時は `setMode` で反転あり、と使い分けが必要。
- 敵の移動時の `targetFor` に渡す自機位置は、**その手で移動した後**の位置。

**次にやるべきこと**
- ランナーの検証結果を確認する。PASS なら本タスクは完了。
- 万一 FAIL なら、まず失敗テスト名から上記「分かったこと」の解釈のどれかを見直す。


- [ランナー] 反復 1: 検証 FAIL → エージェント 12 ターン / $0.8869

### 反復 2

**やったこと**
- コードは変更せず、完了条件 1〜5 を 1 つずつ確認した。
- 1: `node --test test/maze.test.mjs test/ghost.test.mjs test/game.test.mjs` を再実行 → 54/54 PASS（fail 0）。
- 2: `package.json` は存在せず、`src/game.mjs` の import は `./maze.mjs` と `./ghost.mjs` のみ。標準機能だけで動く。
- 3: `ls --time-style=full-iso` で更新時刻を確認。`src/game.mjs` だけ 07:29（反復 1 で新規作成）、他の `src/*.mjs` と `test/*.mjs` は 07:18〜07:27 のまま＝未変更。
- 4: 迷路 M10 相当の盤面で 400 手ランダム入力を回し、毎手 `step` 前後の `JSON.stringify(state)` を比較 → 破壊 0 件。
  `createGame` が入力行配列を壊さないこと、同じ種で同じ結果になることも確認。
- 5: 定数 7 個と `createGame` / `step`（および内部の `cornersOf` / `resolveCollisions`）に JSDoc がある。

**分かったこと**
- 仕様との唯一の差は勝利判定の `bite.eaten !== 0` 条件。`test/game.test.mjs` の `M6 = ['#####','#P G#','#####']`（餌 0）を使う 3 テストが
  `status === 'playing'` を期待するため、仕様の素直な「`countPellets === 0` なら won」だけでは初手で won になり落ちる。反復 1 の判断は妥当。
- `step` の非破壊性はテスト内では「入力の状態は変更しない」1 件でしか見ていないが、衝突・敵移動・モード切替を含む長いシナリオでも破壊が無いことを別途確認できた。
- 敵が 5 体以上いる場合、`corner` オブジェクトが複数の敵で共有される（`corners[i % 4]` をそのまま代入）。誰も書き換えないので実害は無い。

**次にやるべきこと**
- 本タスクは完了。次は 04（CLI）へ進む。

- [ランナー] 反復 2: 検証 PASS → エージェント 10 ターン / $0.6260 / 完了宣言あり
